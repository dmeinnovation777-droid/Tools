name: Tests & Windows build

on:
  push:
    branches: ["**"]
    tags: ["v*"]
  pull_request:
  workflow_dispatch:

# Needed so a tagged build can publish the installer as a release asset.
permissions:
  contents: write

jobs:
  test:
    name: Unit tests (Linux)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Run the core test suite
        run: python -m unittest discover -s tests -v

  headless-gui:
    name: Headless GUI smoke test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install tkinter and a virtual display
        run: sudo apt-get update && sudo apt-get install -y python3-tk xvfb
      - name: Backup, both directions
        run: xvfb-run -a -s "-screen 0 1120x860x24" python tests/smoke_gui_autotuner.py
      - name: Lock and Batch, against a stub builder
        run: xvfb-run -a -s "-screen 0 1120x860x24" python tests/smoke_gui_mhd_lock.py
      - name: All three ways into the app
        run: xvfb-run -a -s "-screen 0 1120x860x24" python tests/smoke_gui_suite.py
      - name: The pages stand still when you switch areas
        run: xvfb-run -a -s "-screen 0 1120x880x24" python tests/smoke_gui_layout.py
      - name: The language switch loses nothing
        run: xvfb-run -a -s "-screen 0 1120x880x24" python tests/smoke_gui_language.py
      - name: The window stays alive while it works
        run: xvfb-run -a -s "-screen 0 1120x880x24" python tests/smoke_gui_responsive.py

  build-windows:
    name: Build Windows installer
    runs-on: windows-latest
    needs: [test]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install PyInstaller
        run: python -m pip install --upgrade pip pyinstaller
      - name: Read the suite version
        id: version
        shell: bash
        run: echo "value=$(python -c 'import dme_brand; print(dme_brand.VERSION)')" >> "$GITHUB_OUTPUT"
      # One executable carrying all three programs. Nothing imports the two
      # tool modules at module level, so without --hidden-import PyInstaller
      # leaves them out and the launcher opens but neither tool starts.
      - name: Build the suite
        run: >
          pyinstaller --onefile --windowed --noconfirm --clean
          --name "DME Innovation Tools" --icon assets/dme-icon.ico
          --hidden-import dme_app --hidden-import autotuner_tool --hidden-import mhd_lock_tool
          dme_suite.py
      - name: Prove every tool starts out of the built executable
        shell: bash
        timeout-minutes: 5
        run: python tools/check_frozen_tools.py "dist/DME Innovation Tools.exe"
      - name: Install Inno Setup
        run: choco install innosetup --no-progress -y
      - name: Build the installer
        run: >
          & "${env:ProgramFiles(x86)}\Inno Setup 6\ISCC.exe"
          /DAppVersion=${{ steps.version.outputs.value }}
          installer\dme-innovation-tools.iss
      - name: Upload the installer
        uses: actions/upload-artifact@v4
        with:
          name: DME-Innovation-Tools-Setup-${{ steps.version.outputs.value }}
          path: dist/DME-Innovation-Tools-Setup-*.exe
          if-no-files-found: error
      - name: Upload the plain executable
        uses: actions/upload-artifact@v4
        with:
          name: dme-tools-windows-exe
          path: dist/DME Innovation Tools.exe
          if-no-files-found: error

      # Publishes the installer as a permanent, login-free download.
      # Runs for a pushed tag, or on demand via "Run workflow" - in the latter
      # case gh creates the tag v<version> at the built commit itself.
      - name: Publish the release
        if: startsWith(github.ref, 'refs/tags/v') || github.event_name == 'workflow_dispatch'
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          VERSION: ${{ steps.version.outputs.value }}
        run: |
          set -euo pipefail
          SETUP="dist/DME-Innovation-Tools-Setup-${VERSION}.exe"
          test -f "$SETUP"
          case "${GITHUB_REF}" in
            refs/tags/*) TAG="${GITHUB_REF_NAME}" ;;
            *)           TAG="v${VERSION}" ;;
          esac
          if gh release view "$TAG" >/dev/null 2>&1; then
            gh release upload "$TAG" "$SETUP" --clobber
          else
            gh release create "$TAG" "$SETUP" \
              --target "${GITHUB_SHA}" \
              --title "DME Innovation Tools ${VERSION}" \
              --notes-file installer/RELEASE_NOTES.md
          fi
          echo "Release: https://github.com/${GITHUB_REPOSITORY}/releases/tag/${TAG}"
